# Enable debug logging for springdoc and web
logging.level.org.springdoc=DEBUG
logging.level.org.springframework.web=DEBUG

# Spring Boot application properties for restaurant ordering app
spring.application.name=demo

# MySQL datasource (edit for your local dev or use env vars for prod)
spring.datasource.url=jdbc:mysql://localhost:3306/springapp
spring.datasource.username=root
spring.datasource.password=1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Server port
server.port=8080

# CORS config is handled in controllers

# JWT Configuration
jwt.secret=E9A814C1B90C1FEDE4C3BA3C79965
jwt.expiration=86400000

springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
# For production, set all credentials via environment variables only:
#   JDBC_DATABASE_URL, JDBC_DATABASE_USERNAME, JDBC_DATABASE_PASSWORD, JWT_SECRET
# Enable Swagger OpenAPI generation
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html